o	touch note.txt
o	echo "line1" > note.txt
o	echo "line2" >> note.txt
o	ls -l
o	cat note.txt
o	head -n2 note.txt
o	head -n1 note.txt
o	tail -n1 note.txt
o	ech "line3" >> note.txt
o	echo "line3: >>> note.txt
•	echo "line3" | tee -a note.txt
•	cat note.txt 
•	echo "line4" | tee -a note.txt
•	cat note.txt 
•	tee
•	htop
•	top
•	clear
•	cat /etc/os-release 
•	uname -a
•	uname
•	netstat
•	cd /var/log/
•	ls -l
•	cat dpkg.log
•	tail -n 10 dpkg.log
•	clear
•	tail -n 10 dpkg.log
•	head -n 10 dpkg.log
•	mkdir /tmp/runbook-demo
•	cp /etc/hosts /tmp/runbook-demo/hosts-copy && ls -l /tmp/runbook-demo
•	mkdir /tmp/runbook-demo1
•	cp /etc/hosts /tmp/runbook-demo1/hosts-copy1
•	cd /tmp/runbook-demo1
•	ls -l
•	cat hosts-copy1 
•	history
•	du -sh /var/log
•	df -h
•	iostat
•	free -h
•	cd ..
•	iostat
•	vm_stat
•	ps -o pid,pcpu,pmem,comm
•	ps -o pid,pcpu,pmem,comm -p 13
•	ps -o pid,pcpu,pmem,comm -p
•	ps
•	ps -o pid,pcpu,pmem,comm -p
•	ps -o pid,pcpu,pmem,comm 
•	ps -o pid,pcpu,pmem,comm,tty
•	journalctl -u dpkg.log -n 10
•	history
